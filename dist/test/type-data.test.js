"use strict";
describe('dataTypes', () => {
    it('should must declare', () => {
        let name = 'Rifki Ari';
        let balance = 2000000;
        let isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
        // name = 100; => error
        // balance = 'galih'; => error
        // isVip = 100; => error
    });
});
