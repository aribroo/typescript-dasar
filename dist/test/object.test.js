"use strict";
describe('Object', () => {
    it('should can use object', () => {
        const person = {
            id: 1,
            name: 'Rifki Ari'
        };
        console.log(person);
    });
});
